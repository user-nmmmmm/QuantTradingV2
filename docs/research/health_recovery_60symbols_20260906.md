# 60 币种策略恢复实验：不采用为正式默认

日期：2026-09-06；实验编号：SR-HEALTH-RECOVERY-001。

## 决策

受控恢复机制工程验证通过，但本次固定参数候选的收益和回撤均劣于原方案，**不启用为正式默认、不解锁任何实盘策略、不放宽组合清仓阈值**。
实现保留为默认关闭的研究选项。没有新增独立仓位账本/UI，也没有声称完整仓位管理模块已完成：本次复用既有仓位计算、组合风险及成交后预算核验链路。

## 固定候选与数据

- 保持原 60 币种、10,000 USDT、日线和全部其他策略/成本/组合风控配置不变。
- 请求 2016-01-01 至 2026-08-31；Binance 冻结行情实际从 2017-08-17 开始。2016 年无数据，不补造。
- 使用上轮冻结的 60 份 CSV，逐文件 SHA-256 验证；没有重新下载或更换币种。
- 唯一政策差异：两次试运行失败后，`extended_cooldown` 冷却 90 天，再按正常风险预算的 10% 恢复；普通试运行仍为 25%。
- 该 10% 是风险乘数，不是仓位占比；基础 2% 单笔风险乘以 0.10 为权益 0.2% 的目标风险预算，仍服从其他更严约束和既有成交后容差规则。
- 连续新退出 cohort 达到 3 个且总 R > 0 才恢复正常健康风险乘数；再次失败重新冷却。
- 人工主动锁定、已有 MANUAL_LOCK、组合熔断、总风险/相关性预算、保护止损和退出权限保持不变。
- 参数与判据在运行前写入 `experiment.json`；没有根据结果搜索新的更优参数。已反复观察的历史不能当作独立样本外验证。

## 结果

| 指标 | 原方案 | 90 天 / 10% 恢复候选 |
|---|---:|---:|
| 期末权益 USDT | 21,521.75 | 20,146.62 |
| 总收益率 | 115.22% | 101.47% |
| 年化收益率（含停止后的空仓期） | 8.85% | 8.06% |
| 最大回撤 | 14.73% | 20.18% |
| 利润因子 | 3.03 | 2.42 |
| 夏普 | 0.727 | 0.664 |
| 胜率 | 52.17% | 49.44% |
| 往返交易数 | 161 | 178 |
| 成交记录数 | 324 | 358 |
| 最后成交日 | 2022-09-19 | 2024-01-07 |
| 最终停止原因 | 策略 MANUAL_LOCK | 组合 portfolio_liquidate |

候选多 17 笔往返交易，但期末少 1,375.14 USDT，最大回撤增加 5.45 个百分点。恢复开仓不是恢复盈利能力。

## 发生了什么

1. 2022-08-20：原本进入人工锁定的位置，候选转为延长冷却，截止 2022-11-18。
2. 2022-11-21：状态机在调用时评估到期，进入 0.10 风险乘数试运行；不是保证截止当天一定有订单。
3. 2023-04-22：试运行失败，重新延长冷却。
4. 2023-07-21：再次进入 0.10 风险试运行。
5. 2023-12-22：通过 cohort 门槛，恢复 ACTIVE（健康乘数 1.0，其他组合限制仍有效）。
6. 2023-12-28：组合回撤超过 15%，触发 BLOCK_NEW。
7. 2024-01-07：已有持仓继续波动，组合回撤超过 20%，触发清仓，4 个持仓降至 0，生命周期 `terminated_by_risk`。

20% 是触发阈值，不是连续价格/日线执行/成本下保证恰好止于 20% 的损失上限。
引擎终止后的 967 天仅保留空仓权益展示至 2026-08-31，不代表仍在执行策略。候选资金对账次数因此为 2,335 次，不是 3,302 次。

## 工程验证

- 正式 `tests/` 全量回归：662 passed、1 skipped、46 subtests passed；JUnit 保存于本次输出目录。
- 新恢复测试覆盖截止边界、重启不漂移、重复失败、旧 cohort 隔离、显式人工锁定、无时间戳锚定、非法参数拒绝。
- 初次无路径 pytest 收集到了历史验收恢复目录的同名测试，产生收集冲突；限定正式 `tests/` 后全量通过，未删除历史证据。
- baseline 与上轮 60 币种结果的 equity/trades/benchmark/report_payload 摘要完全一致。
- candidate 与 candidate_replay 四项结果摘要完全一致。
- baseline 对账 3,302 次、candidate 对账 2,335 次，差异项均为 0。
- 候选成交后预算审计含 7 条 `health_risk_multiplier=0.1` 记录，证明研究风险乘数已到达成交后预算核验链路。
- 正式 `config/params.yaml` 未改变；研究脚本在进程内覆盖配置并在 finally 恢复。没有网络账户操作。

## 证据与复现

输出目录：`outputs/health_recovery/60symbols_90days_10pct_20260906/`。

- `experiment.json`：运行前候选、代码哈希、原始数据清单哈希及判据。
- `comparison.json`、`verification.json`：三组精确结果和复现校验。
- `baseline/`、`candidate/`、`candidate_replay/`：净值、成交、配置、生命周期、资金/风险/健康审计。
- `candidate/breaker_audit.csv`：15% 禁开仓及 20% 清仓的实际触发证据。
- `pytest.xml`：全量回归结果。

```powershell
.\.venv\Scripts\python.exe scripts/run_health_recovery_experiment.py --source outputs/expanded_universe/20160101_20260831_60symbols --output outputs/health_recovery/new_run
```

必须使用不存在的新输出目录，避免覆盖；脚本全程使用冻结行情，不需要账户凭据。

## 后续方向

不应立即改为不断恢复或取消 20% 清仓。下一轮应先审计试运行通过后从 0.10 到 1.0 的风险恢复幅度、已有持仓的组合减仓效果及样本门槛，再预先定义分级恢复方案和验证标准。完整仓位管理的功能范围、独立样本外准入、历史上下架/借贷可用性仍未完成。
