# P0 自动暂停恢复验收（2026-09-09）

本轮完成自动健康暂停的恢复配置接入、兼容性回归和停机披露修复。工程验收通过；策略仍为 `paused_revalidation`，研究准入不通过。本报告不代表整个 P0–P2 路线的所有研究任务重新完成。

## 问题根因与修复

此前已有可选的延长冷却实现，但正式配置没有启用，运行时使用 `manual_lock` 默认值。达到两次试运行失败后，健康状态进入永久人工锁定，状态机不会随时间自动恢复。原回测在 2022-08-20 进入该状态，最后成交于 2022-09-19。报告中的 `completed` 仅表示引擎完成请求区间，而原有停机天数只统计账户终止，未计入策略健康锁定，容易误解为持续正常运行。

本轮在正式配置明确启用 `extended_cooldown`：重复试运行失败后冷却 90 天，再以正常风险预算的 10% 进入试运行。10% 是风险乘数，不是本金仓位比例。升级恢复正常风险仍需满足原有新退出 cohort 验证条件，时间到期本身不代表验证通过。普通冷却、试运行门槛、手续费、融资、杠杆及组合回撤阈值未改。

显式人工锁定和旧存档中的 `MANUAL_LOCK` 保持锁定，不能通过升级配置或重启绕过。低层策略类仍保留兼容默认值，正式入口需加载应用配置。既有实盘状态需要单独核验后按授权恢复，本次新回测不承担旧账户状态迁移。

健康快照新增自动失败动作、下一恢复时间和是否需要人工恢复。回测新增逐 bar 的 `strategy_activity.csv`，按实际路由策略统计健康暂停；报告分别披露策略暂停天数、账户终止后停机天数、最后成交和运行状态。账户临时 BLOCK_NEW 另在逐 bar 记录，账户停机天数仍表示终止后的尾段。

## 固定数据对照

复用 `reports/revalidation_60_20260908` 的冻结 Binance 60 币种数据，60/60 覆盖；本金 10,000 USDT，2016-01-01 至 2026-06-30（含结束日，UTC），`spot_margin`，保留融资和成本。除健康恢复政策外，脚本核对原冻结配置各节一致。原报告未覆盖或修改。

| 运行 | 期末权益 USDT | 总收益 | 最大回撤 | 成交记录数 | PF |
|---|---:|---:|---:|---:|---:|
| 旧政策对照 | 21,336.20 | 113.36% | 14.73% | 324 | 2.9773 |
| 新恢复政策 | 19,972.92 | 99.73% | 20.18% | 358 | 2.3824 |
| 新政策独立重跑 | 19,972.92 | 99.73% | 20.18% | 358 | 2.3824 |

共三次完整区间运行，其中新政策运行两次；不应表述为新政策三次重跑。新政策两次确定性摘要一致，账本核对和运行前后源码哈希核对通过。

实际恢复与终止时间（UTC）：

- 2022-08-20：进入延长冷却，替代此前的自动永久锁定。
- 2022-11-21：以 0.10 风险乘数进入试运行；冷却截止日为 11 月 18 日，实际状态评估发生于 11 月 21 日。
- 2023-04-22：试运行失败，再次延长冷却。
- 2023-07-21：再次以 0.10 风险乘数试运行。
- 2023-12-22：满足原有验证条件，恢复 ACTIVE。
- 2024-01-07：组合回撤触发 `portfolio_liquidate`，终止交易，最后成交同日。

新结果有 473 个策略健康暂停日，账户终止后有 905 个停机日。期末曲线保留风险停机期现金，运行状态明确为 `account_risk_halted`。20.18% 的实际最大回撤超过 20% 门槛，保留实际清算结果，不提高门槛或关闭熔断以强行延长交易。

## 工程证据与边界

- 定向测试：49 passed、6 subtests passed；涵盖正式配置、重启、恢复时间、低风险试运行、显式人工锁定和旧锁定兼容。
- 完整测试：711 passed、1 skipped、46 subtests passed；2 个警告，覆盖率 86.21%，达到 55% 门槛。
- Ruff 通过；既有关键类型检查三个文件通过；补丁空白检查通过。
- 回测验收七项通过：无新自动人工锁定、观察到低风险恢复、实际后续交易、确定性、会计核对、健康暂停披露、源码未变化。

本轮工程验证通过，不等于研究表现通过。新政策收益下降、最大回撤增大，且仍触发终止级风险。该对照使用已观察历史，是回顾性验证；没有在本轮重新完成整套成本压力、滚动窗口及集中度研究，也不宣称获得全新未见样本。保持 `paused_revalidation`，未启动真实资金交易。

## 交付与复现

- `reports/p0_recovery_20260909/protocol.json`：本次参数、来源、数据和源码冻结记录。
- `reports/p0_recovery_20260909/acceptance.json`：工程验收结果。
- `reports/p0_recovery_20260909/summary.json`：三次运行的指标和生命周期。
- 同目录 `runs/legacy_policy`、`runs/recovery`、`runs/recovery_replay`：各次运行明细和活动记录。
- `reports/p0_recovery_tests_20260909.txt`：完整测试输出。
- `scripts/run_p0_recovery_validation.py`：复现入口；使用新的不存在的输出目录运行，避免覆盖本次证据。

运行示例：`.venv/Scripts/python.exe scripts/run_p0_recovery_validation.py --output reports/p0_recovery_new_run`。
